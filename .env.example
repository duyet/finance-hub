# Finance Hub - Environment Variables Template
# Copy this file to .env for local development
# This is an alternative to .devvars for developers who prefer .env files
# Note: .env is typically used by standard Node.js apps, .devvars by Cloudflare Workers

# ============================================================================
# OAUTH PROVIDER CREDENTIALS
# ============================================================================
# GitHub OAuth - Create app at: https://github.com/settings/developers
# - Application name: Finance Hub (Local)
# - Homepage URL: http://localhost:5174
# - Authorization callback URL: http://localhost:5174/auth/callback/github
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Google OAuth - Create app at: https://console.cloud.google.com/apis/credentials
# - Create OAuth 2.0 credentials
# - Authorized JavaScript origins: http://localhost:5174
# - Authorized redirect URIs: http://localhost:5174/auth/callback/google
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# ============================================================================
# SESSION SECURITY
# ============================================================================
# Generate a secure random secret with: openssl rand -base64 32
# This encrypts session cookies and must be unique per environment
SESSION_SECRET=your_session_secret_here

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# Cloudflare D1 (SQLite) - Local Development
# No additional variables needed for local development
# Initialize database with: wrangler d1 migrations apply DB --local
# Database is configured in wrangler.toml

# ============================================================================
# CLOUDFLARE SERVICES (LOCAL)
# ============================================================================
# R2 Storage, KV Namespaces - Local Development
# These work automatically with `wrangler dev`
# No additional configuration needed for local development
# Production values are set in wrangler.toml

# ============================================================================
# BANK SYNC API INTEGRATIONS (OPTIONAL)
# ============================================================================
# These are only needed if you're testing webhook integrations

# Casso Bank Sync - https://casso.vn
# Register at: https://casso.vn to get API keys
# Used for automatic bank transaction synchronization
CASSO_API_KEY=your_casso_api_key
CASSO_WEBHOOK_SECRET=your_casso_webhook_secret

# SePay Bank Sync - https://sepay.vn
# Register at: https://sepay.vn to get API keys
# Alternative bank transaction synchronization provider
SEPAY_API_KEY=your_sepay_api_key
SEPAY_WEBHOOK_SECRET=your_sepay_webhook_secret

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# Default locale for the application
# Supported values: en, vi
DEFAULT_LOCALE=en

# Base URL for the application (local development)
# Used for OAuth callbacks and email links
APP_URL=http://localhost:5174

# ============================================================================
# PRODUCTION SETTINGS (FOR REFERENCE)
# ============================================================================
# These are NOT used in local development but documented here for reference
# Update wrangler.toml with production values when deploying

# R2 Public URL - Set in wrangler.toml
# Format: https://your-bucket.r2.dev or https://your-custom-domain.com
# R2_PUBLIC_URL=https://your-bucket.r2.dev

# KV Namespace IDs - Set in wrangler.toml
# Run: wrangler kv:namespace create "CACHE" --preview
# Run: wrangler kv:namespace create "CACHE" (for production)
